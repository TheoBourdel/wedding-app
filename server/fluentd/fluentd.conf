<source>
  @type forward
  port 24224
</source>

<match docker.server>
  @type elasticsearch
  @log_level info
  host elasticsearch
  port 9200
  logstash_format true
  logstash_prefix docker
  logstash_dateformat %Y.%m.%d
</match>
